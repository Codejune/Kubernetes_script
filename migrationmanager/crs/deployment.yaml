apiVersion: keti.migration/v1
kind: Deployment
metadata:
  name: deploymenttest
  labels:
    app: ticker
spec:
  replicas: 5
  selector:
    matchLabels:
      app: ticker
  template:
    metadata:
      labels:
        app: ticker
    spec:
      containers:
        - name: ticker
          image: hth0919/ticker:v0.0.4
